intent service有两个好处，
	一方面不需要自己去new Thread了；
	另一方面不需要考虑在什么时候关闭该Service了。

service存在的目的：
	一些耗时的操作不能放在ui线程上
	进行耗时操作时，activity有可能在后台
	则此刻activity所在进程的优先级变低，则整个进程有可能被杀死
	则，后台操作放入到service中，来提高进程的优先级
